Convert the job description's required-skills text below into a JSON array of individual skills.

The text has already been located and cleaned, but it may still contain messy formatting. Return ONLY the JSON array: a list of lowercase strings, no explanation.

RULES
1. Copy each skill exactly as written. Never add, infer, rename, shorten or spell-correct a skill.
2. Skip category labels. A label is a short line that names a group instead of a technology (for example "languages", "backend", "cloud & devops", "tools & databases"). Only the skills listed under a label count.
3. Skills can be separated by commas, pipes, line breaks, or only by spaces. Split a run of words into separate skills, but keep well-known multi-word names together (for example "apache flink", "vector databases", "spring cloud gateway").
4. Join wrapped lines. If a line ends with "," or "&", or the next line is a lone word that completes the previous phrase, treat them as one line. Example: "zero-trust" followed by "networking" is one skill, "zero-trust networking".
5. Split "a & b" into two skills. Keep slash names whole (for example "tcp/ip", "grpc/protobuf").
6. Keep every entry, even when several start with the same word: "oci", "oci vault" and "oci functions" are three separate skills.
7. No exact duplicates. Keep the order of first appearance.
8. Ignore full sentences describing responsibilities or experience level (for example "5+ years designing distributed systems", "ability to lead a team") -- only extract the concrete named skill, if any, out of such a sentence. If a line is pure responsibility/experience prose with no named skill in it, skip the line entirely.

EXAMPLE 1
TEXT:
required skills
python, fastapi, postgresql, docker
must have
kubernetes, aws
EXAMPLE 1 OUTPUT:
["python","fastapi","postgresql","docker","kubernetes","aws"]

EXAMPLE 2
TEXT:
candidates must have strong experience with react, typescript
and node.js. must also have working knowledge of graphql apis.
EXAMPLE 2 OUTPUT:
["react","typescript","node.js","graphql"]

EXAMPLE 3
TEXT:
essential skills:
oci, oci vault, oci functions, tcp/ip
zero-trust
networking
5+ years of professional software engineering experience
EXAMPLE 3 OUTPUT:
["oci","oci vault","oci functions","tcp/ip","zero-trust networking"]

Note on example 3: the "5+ years..." line is responsibility/experience prose with no named skill in it, so it is skipped entirely per rule 8.

The text to convert is given in the next message. Return ONLY the JSON array for that text.
